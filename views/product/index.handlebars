<section>
  <h1>Products</h1>
  {{#each products}}
  <article class="product">
    <h2><a href="/product/{{_id}}">{{productName}}</a></h2>
    <p>{{productDescription}}</p>
  </article>
  {{/each}}
</section>